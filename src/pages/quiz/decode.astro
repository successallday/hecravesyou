---
/* No frontmatter needed */
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Decode His Desire</title>

  <style>
    :root {
      --pink:#e63973;
      --pink-dark:#b1124c;
      --yes:#1e4bb8;
      --yes-shadow:#122d6d;
      --no:#e63973;
      --no-shadow:#8b2449;
      --hero-max:900px;
      --quiz-max:760px;
    }

    body {
      margin:0;
      font-family:Georgia, serif;
      color:#fff;
      background:url("/images/sunset.jpg") no-repeat center center fixed;
      background-size:cover;
    }

    .overlay {
      background:rgba(0,0,0,.6);
      min-height:100vh;
      display:flex;
      flex-direction:column;
      align-items:center;
      padding-bottom:60px;
    }

    /* ---------- HERO ---------- */
    .top-box {
      width:100%;
      max-width:var(--hero-max);
      margin:40px auto 0;
      background:#000;
      border-radius:6px;
      box-shadow:0 2px 10px rgba(0,0,0,.6);
      overflow:hidden;
      display:grid;
      grid-template-columns:1fr 1fr;
    }

    .top-title {
      padding:26px 30px 10px;
      font-size:28px;
    }

    .top-copy {
      padding:0 30px 28px;
    }

    .top-copy p {
      color:#ddd;
      font-size:17px;
      line-height:1.55;
      margin:8px 0;
    }

    .top-image {
      width:100%;
      height:100%;
      object-fit:cover;
    }

    .underline {
      height:5px;
      width:100%;
      max-width:var(--hero-max);
      background:var(--pink);
    }

    .triangle {
      width:0;
      height:0;
      border-left:60px solid transparent;
      border-right:60px solid transparent;
      border-top:35px solid var(--pink-dark);
    }

    /* ---------- QUIZ ---------- */
    .quiz-box {
      background:#111;
      width:92%;
      max-width:var(--quiz-max);
      border-radius:10px;
      padding:50px 28px;
      margin-top:0;
      text-align:center;
      box-shadow:0 4px 15px rgba(0,0,0,.5);
      opacity:0;
      transform:translateY(12px);
      transition:
        opacity 0.8s ease,
        transform 0.8s ease;
    }

    .quiz-box.show {
      opacity:1;
      transform:translateY(0);
    }

    .quiz-box h2 {
      margin:0 0 18px;
      font-size:22px;
    }

    .quiz-box p {
      font-size:18px;
      line-height:1.55;
      margin:0 0 30px;
    }

    /* ---------- BUTTONS ---------- */
    .button-row {
      display:flex;
      gap:18px;
      justify-content:center;
      flex-wrap:wrap;
    }

    .button-row button {
      flex:1 1 45%;
      max-width:220px;
      padding:18px 16px;
      font-size:20px;
      font-weight:700;
      border-radius:14px;
      border:none;
      cursor:pointer;
      color:#fff;
      transition:
        transform .15s ease,
        box-shadow .15s ease,
        opacity .15s ease;
    }

    .button-row button:active {
      transform:translateY(3px);
      box-shadow:0 1px rgba(0,0,0,.4);
    }

    .yes-btn {
      background:linear-gradient(#2a63ff, var(--yes));
      box-shadow:0 6px var(--yes-shadow);
    }

    .no-btn {
      background:linear-gradient(#ff5f9a, var(--no));
      box-shadow:0 6px var(--no-shadow);
    }

    /* ---------- FOOTER ---------- */
    footer {
      background:#000;
      color:#999;
      font-size:13px;
      text-align:center;
      padding:35px 10px 45px;
      width:100%;
      margin-top:40px;
      border-top:1px solid #222;
      line-height:1.5;
    }

    footer a {
      color:var(--pink);
      text-decoration:none;
    }

    footer a:hover {
      text-decoration:underline;
    }

    /* ---------- MOBILE ---------- */
    @media (max-width:768px) {
      .top-box {
        grid-template-columns:1fr;
        margin:0;
        border-radius:0;
      }

      .top-title {
        font-size:22px;
        padding:20px 16px 8px;
      }

      .top-image {
        height:220px;
      }

      .top-copy {
        padding:10px 16px 18px;
      }

      .quiz-box {
        padding:36px 20px 44px;
      }

      .button-row button {
        max-width:none;
        font-size:18px;
      }
    }
  </style>
</head>

<body>
  <div class="overlay">

    <section class="top-box">
      <div>
        <h1 class="top-title">Decode His Desire</h1>
        <div class="top-copy">
          <p>Every man speaks desire in a secret language.</p>
          <p>Answer honestly. Each question brings clarity.</p>
        </div>
      </div>
      <img class="top-image" src="/images/valentines.jpg" alt="Couple">
    </section>

    <div class="underline"></div>
    <div class="triangle"></div>

    <div id="quiz" class="quiz-box"></div>

    <footer>
      © 2025 He Craves You •
      <a href="/privacy">Privacy</a> •
      <a href="/terms">Terms</a> •
      <a href="/contact">Contact</a>
    </footer>

  </div>

  <script is:inline>
    const questions = [
      "Do you feel like he pulls away when you get closer?",
      "Do you wonder if he feels as deeply as you do?",
      "Does he go silent after being affectionate?",
      "Do you replay his words looking for meaning?",
      "Does his attention come in waves?",
      "Ready to decode his desire?"
    ];

    let step = 0;
    let score = 0;

    const quiz = document.getElementById("quiz");

    function render() {
      quiz.classList.remove("show");

      setTimeout(() => {
        if (step < questions.length) {
          quiz.innerHTML = `
            <h2>Question ${step + 1} of ${questions.length}</h2>
            <p>${questions[step]}</p>
            <div class="button-row">
              <button class="yes-btn">YES</button>
              <button class="no-btn">NO</button>
            </div>
          `;
        } else {
          quiz.innerHTML = `
            <h2>Your pattern is clear</h2>
            <p>You answered YES to ${score} out of ${questions.length}.</p>
            <p>This means his desire is being expressed — just not in words.</p>
          `;
        }

        quiz.classList.add("show");
      }, 400);
    }

    document.addEventListener("click", e => {
      if (e.target.classList.contains("yes-btn")) {
        score++;
        step++;
        render();
      }
      if (e.target.classList.contains("no-btn")) {
        step++;
        render();
      }
    });

    window.onload = render;
  </script>
</body>
</html>
